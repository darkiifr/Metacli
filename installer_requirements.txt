# Requirements for the MetaCLI Installer
# These packages are needed to run the installer itself

# GUI framework (built-in with Python)
# tkinter - included with Python standard library

# Windows system integration
pywin32>=306

# Optional: For better error handling and logging
# logging - included with Python standard library
# pathlib - included with Python standard library
# winreg - included with Python standard library

# For packaging the installer into executable
pyinstaller>=5.0

# Note: The installer will automatically install the application dependencies:
# click>=8.0.0
# Pillow>=9.0.0
# mutagen>=1.45.0
# PyPDF2>=3.0.0
# python-docx>=0.8.11
# pyyaml>=6.0
# tabulate>=0.9.0
# openpyxl>=3.0.0
# chardet>=5.0.0